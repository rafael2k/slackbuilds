#!/bin/bash
#
# build script for perl-archive-zip
# by rhatto at riseup.net by rhatto | gpl
# build it with slacktrack, example:
#
#   ARCH=x86_64 slacktrack -jefkzp "perl-archive-zip-2.001-x86_64-1rha.tgz" "./perl-archive-zip.build"
# 

CWD="`pwd`"

if [ -s "slack-required" ]; then
  echo Recomended and required packages for building ekiga are:
  cat slack-required | sed -e 's/^/\t/'
  if [ "$INTERACT" != "no" ]; then
    echo If you dont have those installed, press Ctrl-C. Otherwise, hit ENTER.
    read crap
#  else
#    echo Sleeping 3 seconds...
#    sleep 3
  fi
fi

if [ -f "/etc/slackbuildrc" ]; then
  source /etc/slackbuildrc
fi

if [ -f "~/.slackbuildrc" ]; then
  source ~/.slackbuildrc
fi

# default settings
NAME="Archive::Zip"
PACKAGE="perl-`echo $NAME | sed -e 's/::/-/g' | tr '[:upper:]' '[:lower:]'`"
TMP=${TMP:=/tmp}
VERSION=${VERSION:=1.18}
ARCH=${ARCH:=i386}
BUILD=${BUILD:=1rha}
SRC_DIR=${SRC:=$CWD}

# -------  error codes for createpkg  --------------
ERROR_WGET=31;      ERROR_MAKE=32;      ERROR_INSTALL=33
ERROR_MD5=34;       ERROR_CONF=35;      ERROR_HELP=36
ERROR_TAR=37;       ERROR_MKPKG=38;     ERROR_GPG=39
ERROR_PATCH=40

if [ "$ARCH" == "x86_64" ]; then
  LIBDIR=/usr/lib64
else
  LIBDIR=/usr/lib
fi

RTOOL="wget"
PACKAGE_EXT="gz"
SRC="$PACKAGE-$VERSION.tar.$PACKAGE_EXT"
FILE_NAME="`echo $NAME | sed -e 's/::/-/g'`"
SRC="$FILE_NAME-$VERSION.tar.gz"
URL="http://search.cpan.org/CPAN/authors/id/A/AD/ADAMK/$SRC"

SRC_DIR="$SRC_DIR/$PACKAGE"

if [ ! -d "$SRC_DIR" ]; then
  mkdir -p $SRC_DIR
fi

if [ "$RTOOL" == "wget" ] && [ ! -f "$SRC_DIR/$SRC" ]; then
  wget "$URL" -O "$SRC_DIR/$SRC" || exit $ERROR_WGET
fi

TMP="$TMP/$PACKAGE"

if [ ! -d "$TMP" ]; then
  mkdir -p $TMP
fi

tar xvf $SRC_DIR/$SRC || exit $ERROR_TAR
cd $FILE_NAME-$VERSION

# config and install!
mkdir -p /install

perl Makefile.PL
make
make test
make install

# make the package

cat << EOF > /install/slack-desc
# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|'
# on the right side marks the last column you can put a character in.  You must
# make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':'.

                 |-----handy-ruler-----------------------------------------------------|
perl-archive-zip: perl-archive-zip (Provide an interface to ZIP archive files)
perl-archive-zip:
perl-archive-zip:
perl-archive-zip:
perl-archive-zip:
perl-archive-zip:
perl-archive-zip:
perl-archive-zip:
perl-archive-zip:
perl-archive-zip:
perl-archive-zip:
EOF

